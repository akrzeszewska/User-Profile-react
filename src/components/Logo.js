import React from "react";
import classes from "./Logo.module.css";

export const Logo = () => {
    return (
        <div className={classes.logo}>
            <div className={classes.logoIn}></div>
        </div>

    );
}